/** @deprecated This shell is no longer used after the v2 redesign. Navigation now uses bottom tabs + sidebar. */
export function AppShell({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
